#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0   */
char *_strcat(char *dest, char *src)
{
int i = 0 ;
int destLength = 0;
while(dest[i++]){
  
destLength++;
for (i = 0,src[i],i++)
dest[destLength++] = src[i];
 return dest;

} 
